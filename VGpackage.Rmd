---
title: "Video_Game_Analysis"
author: "Travis"
date: "2025-04-13"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

Setup
```{r, "Setup"}
#All libraries go here
```

1. Create a function that imports and cleans data
```{r, "Data Import & Clean}
#inputs here will be data files and returns the data

#Also preferably clean the data as well by dealing with missing values, eliminating duplicates, and possibly transforming data types or filtering out outliers.

# Return a cleaned data source. May need to be its own function.
```

2. Calculate the metrics of the data: active users (daily/monthly), revenue. 
```{r, "Metrics}
# Here I would like to input data, then calculate the daily users of a game, monthly users of a game,
# and revenue of a game.
# Return revenue, daily users, and monthly users


```


3. Finally plot the data
```{r, Plots}
#Input data will be from previous metric steps.
#visualize and plot (most likely line chart) the revenue and playerbase.

```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.
